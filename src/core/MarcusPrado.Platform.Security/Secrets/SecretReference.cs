namespace MarcusPrado.Platform.Security.Secrets;

/// <summary>Holds the logical name and optional version of a secret so it can be resolved at runtime via <see cref="ISecretProvider"/>.</summary>
public class SecretReference { }
